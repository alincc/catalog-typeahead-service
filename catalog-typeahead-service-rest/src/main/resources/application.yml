info:
  app:
    name: catalog-typeahead-service
    description: Gives search suggestions based on other searches

endpoints:
  restart:
    enabled: true
  refresh:
    enabled: true

server:
  port: 8012

spring:  

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

elastic:
  hosts:
    - localhost
  index: suggestioncounters

---
spring:
    profiles: docker

server:
  port: 8080

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/

catalog:
  esHosts:
    - elasticsearch